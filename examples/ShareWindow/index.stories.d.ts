import { Meta, StoryObj } from '@storybook/react';
import { default as ShareWindow } from '.';

declare const meta: Meta<typeof ShareWindow>;
export default meta;
type Story = StoryObj<typeof meta>;
export declare const View: Story;
