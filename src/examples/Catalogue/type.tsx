interface CatalogueData {
  sizes: string[];
  selectedSize: string;
}

interface SpecificationData {
  title: string;
  productName: string;
  productPrice: string;
  productDetail: string;
  material: string;
  sizes: string;
}
